# This file is intentionally minimal.
# All runtime and optional dependencies are managed via pyproject.toml extras.
# Install with one of:
#   pip install -e .            (core only)
#   pip install -e ".[dev]"      (development tools)
#   pip install -e ".[rag]"      (RAG + embeddings)
#   pip install -e ".[llm]"      (local LLM inference)
#   pip install -e ".[games]"    (game generation)
#   pip install -e ".[api]"      (observability FastAPI)
#
# See pyproject.toml [project.optional-dependencies] for the full list.
requests>=2.30
